import SolutionPreview from "@/components/scheduling/SolutionPreview";

export const metadata = {
  title: "TFM — Preview de Solución",
};

export default function PreviewPage() {
  return <SolutionPreview />;
}
